{
	"name": "Startup Profile (Docker)",
	"dockerComposeFile": [
		"../docker-compose.yml"
	],
	"service": "laravel.test",
	"workspaceFolder": "/var/www/html",
	"customizations": {
		"vscode": {
			"extensions": [
				"bmewburn.vscode-intelephense-client",
				"mikestead.dotenv",
				"bradlc.vscode-tailwindcss"
			]
		}
	},
	"remoteUser": "sail",
	"postCreateCommand": "sudo chown -R sail:sail /var/www/html && composer install && npm install && cp -n .env.example .env || true && php artisan key:generate && touch database/database.sqlite && php artisan migrate --force"
}
